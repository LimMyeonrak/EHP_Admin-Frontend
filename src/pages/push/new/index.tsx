import React from 'react';
import Container from './NewPushContainer';

const PushNew: React.FunctionComponent = () => {
    const Header = {
        title: ''
    };

    return <Container />;
};

export default PushNew;
