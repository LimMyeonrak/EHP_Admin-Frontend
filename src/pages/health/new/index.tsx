import React from 'react';
import Container from './NewHealthContainer';

const HealthNew: React.FunctionComponent = () => {
    const Header = {
        title: ''
    };

    return <Container />;
};

export default HealthNew;
