import React from 'react';
import Container from './NewTermsContainer';

const TermsNew: React.FunctionComponent = () => {
    const Header = {
        title: ''
    };

    return <Container />;
};

export default TermsNew;
